#Bruce diaz
#this program will write a function to check if number falls in given range

def test_range(n):
    if n in range(0,11):
        print("%s is in the range"%str(n))
    else:
        print("the number is outside the given range.")
            
